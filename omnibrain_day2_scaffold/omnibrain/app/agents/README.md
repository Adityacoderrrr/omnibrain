# Agents package

Agentic orchestration package — LangGraph supervisor and specialist agents.

Populated starting **Week 2**:

- `supervisor.py` — LangGraph state graph + Supervisor node (routing logic)
- `search_agent.py` — semantic retrieval over the Qdrant text collection
- `vision_agent.py` — VLM-based table/chart reasoning
- `sql_agent.py` — text-to-SQL over historical structured data
